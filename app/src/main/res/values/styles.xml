<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="fontPath">Prompt-Bold.ttf</item>
        <item name="android:textViewStyle">@style/Theme.AppCompat.Light.DarkActionBar.TextView
        </item>
    </style>

    <style name="AppTheme.TranslucentStatusBar" parent="@style/AppTheme">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="Theme.AppCompat.Light.DarkActionBar.TextView" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="fontPath">Prompt-Bold.ttf</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="TextTopic">
        <item name="android:textSize">26dp</item>
        <item name="android:textColor">@color/colorNormalText</item>
        <item name="fontPath">DB HelvethaicaMon X Bd.ttf</item>
    </style>

    <style name="foodNameItem">
        <item name="android:textSize">22sp</item>
        <item name="android:layout_marginTop">4dp</item>
        <item name="android:layout_marginBottom">0dp</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/colorNormalText</item>
    </style>

    <style name="foodDescriptionItem">
        <item name="android:textSize">18sp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:textColor">@color/colorSubStandardGrey</item>
        <item name="fontPath">DB HelvethaicaMon X.ttf</item>
        <item name="android:lineSpacingExtra">13sp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">28sp</item>
    </style>

    <style name="NavDrawerTextStyle">
        <item name="android:paddingLeft">80dp</item>
        <item name="fontPath">DB HelvethaicaMon X Bd.ttf</item>
    </style>

    <style name="Theme.Custom" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="CustomActionBar" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:windowContentOverlay">@null</item>

    </style>

</resources>
